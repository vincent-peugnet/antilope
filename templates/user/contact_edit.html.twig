{% extends 'base.html.twig' %}

{% block title %}{{ 'edit contact'|trans|capitalize }}{% endblock %}

{% block content %}


{% import 'macros/breadcrumbs.html.twig' as breadcrumbs %}

{{ breadcrumbs.render([
    {'url': url('user'), 'text': 'users'|trans|capitalize },
    {'url': url('user_show', {id: user.id}), 'text': user.username},
    {'url': url('user_contact', {id: user.id}), 'text': 'contacts'|trans|capitalize },
    {'text': 'edit'|trans|capitalize },
]) }}


<h1>{{ 'edit contact'|trans|capitalize }}</h1>

{{ form(form) }}



{% endblock %}