{% extends 'base.html.twig' %}


{% block content %}


{% import 'macros/breadcrumbs.html.twig' as breadcrumbs %}

{{ breadcrumbs.render([
    {'url': url('user'), 'text': 'Users'},
    {'url': url('user_show', {id: user.id}), 'text': user.username},
    {'url': url('user_contact', {id: user.id}), 'text': 'contacts'},
    {'text': 'new'},
]) }}


<h1>New contact</h1>

{{ form(form) }}



{% endblock %}