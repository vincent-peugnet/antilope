{% extends 'base.html.twig' %}


{% block title %}{{ sharable.name }}{% endblock %}

{% block content %}

<a href="{{ path('sharable') }}">sharables</a> > <a href="{{ path('sharable_show', {id: sharable.id}) }}">{{ sharable.name }}</a> > edit

<hr>

{{ form(form) }}


{% endblock %}