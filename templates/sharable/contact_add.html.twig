{% extends 'base.html.twig' %}


{% block title %}{{ sharable.name }} : {{ 'new contact'|trans|capitalize }}{% endblock %}

{% block content %}

{% import 'macros/breadcrumbs.html.twig' as breadcrumbs %}

{{ breadcrumbs.render([
    {'url': url('sharable'), 'text': 'sharables'|trans|capitalize },
    {'url': url('sharable_show', {id: sharable.id}), 'text': sharable.name},
    {'url': url('sharable_contact', {id: sharable.id}), 'text': 'contact'|trans|capitalize },
    {'text': 'new'|trans|capitalize },
]) }}

{{ form(form) }}

{% endblock %}